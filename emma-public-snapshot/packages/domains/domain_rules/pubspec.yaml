name: domain_rules
description: Rules domain — tariff and rule set entities.
version: 0.1.0
resolution: workspace

environment:
  sdk: ^3.11.0

dependencies:
  freezed_annotation: ^3.1.0
  meta: any
  yaml: ^3.1.2

dev_dependencies:
  build_runner: any
  freezed: any
  json_serializable: any
  test: any
