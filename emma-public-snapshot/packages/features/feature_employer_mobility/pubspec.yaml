name: feature_employer_mobility
description: Employer-mobility feature — budget dashboard, benefits, job tickets, profile mode switch.
version: 0.1.0
resolution: workspace

environment:
  sdk: ^3.11.0

dependencies:
  flutter:
    sdk: flutter
  # Workspace-interne Packages
  domain_employer_mobility: any
  domain_wallet: any
  emma_contracts: any
  # Externe Dependencies
  dio: ^5.8.0+1
  flutter_riverpod: ^3.0.3
  flutter_secure_storage: ^9.2.4

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: any

flutter:
  uses-material-design: true
