# packages

Gemeinsame Pakete sind grob in `core`, `domains`, `features` und `adapters`
geschnitten. Diese Struktur ist bewusst schlanker als eine vollstaendig
feingranulare Paketzerlegung und laesst sich spaeter weiter ausdifferenzieren.
