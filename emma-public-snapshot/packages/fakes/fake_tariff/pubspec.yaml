name: fake_tariff
description: |
  Fake TariffPort (M11) — YAML-Fixture + domain_rules-Engine, ADR-03-konform.
version: 0.1.0
resolution: workspace

environment:
  sdk: ^3.11.0

dependencies:
  domain_rules: any
  emma_contracts: any

dev_dependencies:
  test: any
