name: fake_ticketing
description: In-memory fake for domain_ticketing (MVP fakes).
version: 0.1.0
resolution: workspace

environment:
  sdk: ^3.11.0

dependencies:
  domain_ticketing: any

dev_dependencies:
  test: any
