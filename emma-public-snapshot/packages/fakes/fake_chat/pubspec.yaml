name: fake_chat
description: Fake ChatPort implementation with keyword-templated replies.
  MVP default under ADR-03 / ADR-05.
version: 0.1.0
resolution: workspace

environment:
  sdk: ^3.11.0

dependencies:
  emma_contracts: any

dev_dependencies:
  test: any
