name: fake_settings
description: Fake ConsentSettingsPort for MVP (in-memory, no paid API).
version: 0.1.0
resolution: workspace

environment:
  sdk: ^3.11.0

dependencies:
  emma_contracts: any

dev_dependencies:
  test: any
